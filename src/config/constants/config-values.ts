import { Config } from '../types/config-types';

/**
 * * Contains the default values for the keys used with `remote config`.
 */
export const DefaultConfig: Config = {
  awesome_new_feature: 'disabled',
};
